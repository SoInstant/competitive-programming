#include <bits/stdc++.h>
using namespace std;

#define int long long // ~long long man~

int r, c;

int32_t main() {
	ios::sync_with_stdio(0);
	cin.tie(0);
	cin >> r >> c;
	int dp[r][r];
	memset(dp, 1, sizeof dp);
	return 0;
}
