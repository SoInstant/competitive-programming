#include <bits/stdc++.h>
using namespace std;

#define int long long // ~~~LONG LONG MAN~~~

string a, b;

int32_t main() {
	ios::sync_with_stdio(0);
  	cin.tie(0);
  	cout.tie(0);
	cin >> a >> b;
	int x = a.size(), y = b.size();
	int dp[x][y];
	
  	return 0;
}
