#include <bits/stdc++.h>
using namespace std;

#define int long long

int32_t main() {
	ios::sync_with_stdio(0);
	cin.tie(0);
	int a,b;
    cin >> a;
    cin >> b;
    if (a < b) cout << b;
    else cout << a;
	return 0;
}
