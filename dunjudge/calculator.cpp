#include <bits/stdc++.h>
using namespace std;

#define int long long

int32_t main() {
	ios::sync_with_stdio(0);
	cin.tie(0);
	int a,b;
    string c;
    cin >> a >> c >> b;
    if (c=="+") {cout << a +b;}
    else if (c=="-") {cout << a-b;}
    else if (c=="*") {cout << a*b;}
	return 0;
}

	
