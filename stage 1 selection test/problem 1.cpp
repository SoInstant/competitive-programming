#include <bits/stdc++.h>
using namespace std;

#define int long long // ~long long man~

int n, a, b;

int32_t main() {
	ios::sync_with_stdio(0);
	cin.tie(0);
	cout.tie(0);
	cin >> n;
	if (n == 1) {cout << "Hello World";}
	else {
		cin >> a>> b;
		cout << a*b;
	}
	return 0;
}
