#include <bits/stdc++.h>
using namespace std;

#define int long long // ~~~LONG LONG MAN~~~

int n, k;

int32_t main() {
	ios::sync_with_stdio(0);
  	cin.tie(0);
  	cout.tie(0);
	cin >> n >> k;
	int p1[n], p2[n], dp[n], table;
	for (int i = 0; i < n; ++i) {cin >> p1[i];}
	for (int i = 0; i < n; ++i) {cin >> p2[i];}
	if (p1)
  	return 0;
}
